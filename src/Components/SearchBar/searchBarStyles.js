import { StyleSheet } from "react-native";

export default StyleSheet.create({
    searchContainer:{
        backgroundColor: '#eceff1',
        padding: 10,
        borderRadius: 10,
        marginBottom: 10,
        marginTop: 10,

    },
    searchContainerText:{

    },
});